import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.miempresa.gestoroperaciones',
  appName: 'GestordeOperaciones',
  webDir: 'build'
};

export default config;
